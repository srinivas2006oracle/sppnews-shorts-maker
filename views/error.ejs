<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Error</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f8f8f8; }
    .error-container {
      max-width: 500px;
      margin: 80px auto;
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      padding: 32px 24px;
      text-align: center;
    }
    .error-message {
      color: #c0392b;
      font-size: 1.3em;
      margin-bottom: 24px;
    }
    .back-link {
      display: inline-block;
      margin-top: 16px;
      padding: 10px 22px;
      background: #2d5072;
      color: #fff;
      border-radius: 4px;
      text-decoration: none;
      font-weight: bold;
      transition: background 0.2s;
    }
    .back-link:hover {
      background: #1a344a;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <div class="error-message"><%= error %></div>
    <div class="error-message-telugu"><%= errorTelugu %></div>
    <a href="/" class="back-link">Back to Home</a>
  </div>
</body>
</html>
